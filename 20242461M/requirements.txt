# Track A -- data & persistence
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
nltk>=3.8

# Notebook environment
jupyterlab>=4.0
ipykernel>=6.0

# Testing. scikit-learn is used only to cross-check the hand-written metrics
# and fold splitter in tests/test_evaluation.py; nothing in src/ imports it.
pytest>=7.4
scikit-learn>=1.3

# Added by later tracks (B: classical ML, C: deep learning, D: baseline)
# scikit-learn>=1.3
# torch>=2.0
# transformers>=4.35
# setfit>=1.0
# sentence-transformers>=2.2
